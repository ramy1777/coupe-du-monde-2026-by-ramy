import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.glassify.worldcup',
  appName: 'Coupe du Monde Glassify',
  webDir: 'dist'
};

export default config;
